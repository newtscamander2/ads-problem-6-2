\documentclass{article}

\usepackage[a4paper]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[noend]{algpseudocode}
\usepackage{amsthm}
\usepackage{colortbl}
\usepackage{graphicx}
\usepackage{lineno}
\usepackage[textwidth=4cm]{todonotes}
\usepackage{float}

\geometry{left=3cm, right=6cm, top=3cm, bottom=3cm, marginparwidth=4cm} 

\linenumbers

\setuptodonotes{noline, color=green!15, size=\scriptsize}

\newtheorem*{exercise}{Exercise}
\newcommand{\grey}{\cellcolor{black!25}}

\title{Algorithms and Data Structures \\
  Handin 3: D'ary heap}

\author{
    202605673 Lukas Alstrup \\
    202605116 Thomas Seeberg Hansen \\
    202606296 Frederik Dommer Løvbjerg Plum
}

%\date{September 1, 2025}  % Use \date to set a specific date

\begin{document}

\maketitle
\begin{exercise}[D'ary heap]
	A d-ary heap is like a binary heap, but (with one possible exception)
	nonleaf nodes have d children instead of two children. In all parts of
	this problem, assume that the time to maintain the mapping between
	objects and heap elements is O(1) per operation.

	a. Describe how to represent a d-ary heap in an array.

	b. Using $\theta$-notation, express the height of a d-ary heap of n elements in
	terms of n and d.

	c. Give an efficient implementation of EXTRACT-MAX in a d-ary
	max-heap. Analyze its running time in terms of d and n.

	d. Give an efficient implementation of INCREASE-KEY in a d-ary
	max-heap. Analyze its running time in terms of d and n.

	e. Give an efficient implementation of INSERT in a d-ary max-heap.
	Analyze its running time in terms of d and n.

\end{exercise}


\section*{Comment}

\section*{Our solution}

\section*{a}
A d'ary array can be represented using its children. For the left child of a given node, the index of the child is $d \cdot i$, and for the right child, the index is one larger than the left child, meaning $d \cdot i + 1$.

$$i \cdot d$$
$$i \cdot d + 1$$

\section*{b}
To find the height of a d'ary heap in terms of $n$ and $d$ where $n$ is the number of elements in the sequence and $d$ is the maximum amount of children.
$$h = \log_d{n}$$

\section*{c}
%Insert pseudo code here
\begin{center}
	\begin{minipage}{8.8cm}
		\begin{algorithmic}
			\Procedure{Search}{$A, y$}

			\Comment{test}
			\State{$i = 1$}
			\While{$A[i] < y$}
			\EndWhile{}

			\EndProcedure
		\end{algorithmic}
	\end{minipage}
\end{center}






%Max-heap-maximum
Max-Heap-Maximum(A)

if A.heap-size < 1

error

return A[1]

%Max heapify
Max-heapify(A, i)

for x =  0 to d-1

index = i + i * d + x

if index <= A.heap-size and A[index] > A[i]

largest = index

else largest = i


if largest != i
Exchange A[i] with A[largest]
Max-heapify(A, largest)

%Extract max
Extract-max(A)

max = Max-Heap-Maximum(A)

A[1] = A[a.heap-size]

Max-heapify(A, 1)

return max


\section*{d}

\section*{e}

\end{document}
